# RDFS Reasoning: Вывод через подсвойства
#
# 🔥 АВТОМАТИЧЕСКИЙ ВЫВОД - УНИКАЛЬНАЯ ВОЗМОЖНОСТЬ RDF!
# В SQL нужны триггеры или хранимые процедуры
#
# Определяем правило ОДИН РАЗ:
# :usesMicroservices rdfs:subPropertyOf :requiresOrchestration
# :requiresOrchestration rdfs:subPropertyOf :requiresInfrastructure
#
# Теперь запрос "что требует инфраструктуру" автоматически
# включает всё, что использует микросервисы!
#
# Ожидаемый результат: ADR с автоматически выведенными требованиями
# Время: < 1 секунда

PREFIX : <http://example.org/adr#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?adrLabel ?requirement ?value WHERE {
    ?adr rdfs:label ?adrLabel ;
         :requiresInfrastructure ?value .
    
    BIND("требует инфраструктуру" AS ?requirement)
}
ORDER BY ?adrLabel

# Этот запрос найдёт:
# - ADR с прямым :requiresInfrastructure
# - ADR с :requiresOrchestration (автоматически выводится!)
# - ADR с :usesMicroservices (тоже автоматически!)
#
# В SQL для этого нужен триггер на INSERT/UPDATE
# который вручную добавляет записи в таблицу requirements

