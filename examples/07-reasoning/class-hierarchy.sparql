# Иерархия классов (RDFS Reasoning)
#
# Продемонстрировать автоматический вывод типов через rdfs:subClassOf.
#
# Ожидаемый результат: 7 пар (тип, супертип) для каждого ADR
# Примечание: Каждый ADR является и :ADR, и :ArchitecturalDecision
# Время выполнения: < 1 секунды

PREFIX : <http://example.org/adr#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?adr ?type
WHERE {
    ?adr a :ArchitecturalDecision ;    # Искать экземпляры родительского класса
         a ?type .                     # Получить все типы (включая выведенные)
}
ORDER BY ?adr ?type                    # Сортировать по ADR и типу

# Пояснение:
# В онтологии определено: :ADR rdfs:subClassOf :ArchitecturalDecision
# GraphDB с включённым RDFS reasoning автоматически выводит:
# если (?x a :ADR), то (?x a :ArchitecturalDecision)
# 
# Каждый ADR имеет оба типа:
# - Явно заявленный: :ADR
# - Автоматически выведенный: :ArchitecturalDecision
# 
# В SQL: потребовались бы триггеры, представления или сложная нормализация.
# В Property Graph: дублирование меток или отдельные узлы.
# В RDF с reasoning: автоматическая дедукция на основе онтологии!
