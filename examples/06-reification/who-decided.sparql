# Кто принял решение использовать технологию?
#
# 🔥 REIFICATION - УНИКАЛЬНАЯ ВОЗМОЖНОСТЬ RDF!
# Метаданные О ФАКТАХ: кто сказал, когда, с какой уверенностью
#
# В SQL для этого нужна отдельная таблица statement_metadata
# с foreign keys и сложными JOIN'ами
#
# В RDF это естественная концепция!
#
# Ожидаемый результат: Кто принял каждое решение о технологии
# Время: < 1 секунда

PREFIX : <http://example.org/adr#>
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?adrLabel ?techLabel ?personName ?date ?confidence ?rationale
WHERE {
    # Найти reified statement (утверждение о решении)
    ?stmt a rdf:Statement ;
          rdf:subject ?adr ;
          rdf:predicate :decidesTechnology ;
          rdf:object ?tech ;
          # Метаданные об утверждении
          :statedBy ?person ;
          :statedOn ?date ;
          :confidence ?confidence .
    
    # Получить читаемые имена
    ?adr rdfs:label ?adrLabel .
    ?tech rdfs:label ?techLabel .
    ?person rdfs:label ?personName .
    
    # Опциональное обоснование
    OPTIONAL { ?stmt :decisionRationale ?rationale }
}
ORDER BY DESC(?date)

# Результат покажет:
# ADR-001 | Kafka | Иван Петров | 2024-12-15 | 0.95 | "Необходима потоковая обработка..."
# ADR-007 | Kubernetes | Дмитрий Волков | 2024-10-01 | 0.98 | "Стандарт индустрии..."
# ADR-005 | PostgreSQL | Мария Сидорова | 2024-06-25 | 0.90 | "ACID гарантии..."
#
# ЭТО НЕВОЗМОЖНО ВЫРАЗИТЬ ЭЛЕГАНТНО В SQL!

