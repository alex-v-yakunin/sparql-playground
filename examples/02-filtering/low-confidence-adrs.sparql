# ADRs with Low Confidence
#
# Find architectural decisions with confidence level < 0.70.
#
# Expected result: 3 ADRs (ADR-004: 0.45, ADR-006: 0.65, ADR-008: 0.62)
# Execution time: < 1 second

PREFIX : <http://example.org/adr#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?adr ?label ?confidence ?status
WHERE {
    ?adr a :ADR ;                      # Find all ADRs
         rdfs:label ?label ;           # Get label
         :hasConfidence ?confidence ;  # Get confidence level
         :hasStatus ?status .          # Get status
    
    FILTER(?confidence < 0.70)         # Filter: confidence < 70%
}
ORDER BY ?confidence                   # Sort from lowest to highest

# Explanation:
# These decisions may require additional validation or review.
# In SQL: WHERE confidence < 0.70
# In SPARQL: FILTER with numeric comparison.
