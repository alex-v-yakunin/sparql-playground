# Упрощённое представление (CONSTRUCT)
#
# Создать упрощённый граф только с ключевой информацией для визуализации.
#
# Ожидаемый результат: Новый граф с 7 ADR и их основными свойствами
# Время выполнения: < 1 секунды

PREFIX : <http://example.org/adr#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

CONSTRUCT {
    ?adr rdfs:label ?label ;           # Сохранить название
         :technology ?technologyName ; # Упрощённое свойство с названием
         :status ?statusLabel .        # Упрощённое свойство со статусом
}
WHERE {
    ?adr a :ADR ;                      # Найти все ADR
         rdfs:label ?label ;           # Получить название
         :hasStatus ?status ;          # Получить статус (URI)
         :decidesTechnology ?tech .    # Получить технологию (URI)
    
    ?tech rdfs:label ?technologyName . # Название технологии
    ?status rdfs:label ?statusLabel .  # Название статуса
}

# Пояснение:
# CONSTRUCT создаёт новый RDF граф вместо таблицы результатов.
# Блок CONSTRUCT { ... } определяет, какие триплеты создать.
# Блок WHERE { ... } находит данные-источники.
# 
# Упрощаем граф:
# - Вместо URI технологий и статусов используем их названия
# - Убираем сложные свойства, оставляя только ключевые
# - Идеально для визуализации или экспорта
# 
# В SQL: CREATE VIEW с упрощёнными JOIN.
# В Property Graph: projection графа.
# В SPARQL: CONSTRUCT - мощный инструмент трансформации!
