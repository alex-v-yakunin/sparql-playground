# Обратные зависимости (кто зависит от Linux)
#
# Найти все технологии, которые прямо или косвенно зависят от конкретной технологии.
#
# Ожидаемый результат: 5 технологий, зависящих от Linux
# Время выполнения: < 1 секунды

PREFIX : <http://example.org/adr#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT DISTINCT ?dependent
WHERE {
    ?tech ^:dependsOn+ :Linux ;        # Обратный транзитивный путь от :Linux
          rdfs:label ?dependent .      # Получить название зависимой технологии
}
ORDER BY ?dependent                    # Сортировать по названию

# Пояснение:
# Property Path ^ означает "обратное направление" (от объекта к субъекту).
# ^:dependsOn+ читается как "один или более шагов в обратном направлении по :dependsOn".
# Ищет все технологии, для которых есть путь зависимости до :Linux.
# В SQL: рекурсивный CTE с обратным JOIN.
# В Property Graph: обход по входящим рёбрам.
# Критично для анализа влияния: если Linux недоступен, какие технологии пострадают?
