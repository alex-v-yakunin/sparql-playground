# Системы и их владельцы
#
# Список всех систем с командами, которые ими владеют.
# Демонстрирует связь между сущностями через свойство :ownedBy.
#
# В SQL: SELECT * FROM systems JOIN teams ON systems.owner_id = teams.id
# В SPARQL: естественный граф связей, JOIN не нужен
#
# Ожидаемый результат: 5 систем с информацией о командах-владельцах
# Время: < 1 секунды

PREFIX : <http://example.org/adr#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?system ?systemLabel ?team ?teamLabel
WHERE {
    ?system a :System ;
            rdfs:label ?systemLabel ;
            :ownedBy ?team .
    
    ?team rdfs:label ?teamLabel .
}
ORDER BY ?systemLabel

