# Список всех ADR
#
# Получить все Architecture Decision Records с их названиями.
#
# Ожидаемый результат: 8 ADR
# Время выполнения: < 1 секунды

PREFIX : <http://example.org/adr#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?adr ?label                  # Возвращаем URI ADR и его название
WHERE {
    ?adr a :ADR ;                   # Найти все сущности типа :ADR
         rdfs:label ?label .        # Получить их метку (название)
}
ORDER BY ?adr                       # Сортировать по URI

# Пояснение:
# Паттерн "?adr a :ADR" находит все сущности типа ADR.
# "a" - это сокращение для предиката rdf:type.
# В SQL: SELECT * FROM adr ORDER BY id
# В SPARQL: фильтрация по типу естественна для графовой модели.
