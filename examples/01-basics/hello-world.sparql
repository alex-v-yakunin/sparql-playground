# Hello World: Первый SPARQL-запрос
#
# Возвращает первые 10 троек (триплов) из базы данных.
# Тройка = (субъект, предикат, объект) - базовая единица данных в RDF.
#
# В SQL: аналог "SELECT * FROM table LIMIT 10"
# В SPARQL: работаем с графом, а не с таблицами
#
# Ожидаемый результат: 10 троек, показывающих структуру данных
# Время: < 1 секунды

SELECT ?subject ?predicate ?object
WHERE {
    ?subject ?predicate ?object .
}
LIMIT 10

