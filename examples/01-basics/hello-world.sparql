# Hello World: Первый SPARQL-запрос
#
# Возвращает первые 10 триплетов из базы данных для ознакомления со структурой.
#
# Ожидаемый результат: 10 триплетов (subject, predicate, object)
# Время выполнения: < 1 секунды

SELECT ?subject ?predicate ?object  # Выбираем три переменные
WHERE {
    ?subject ?predicate ?object .   # Паттерн: любой триплет (s, p, o)
}
LIMIT 10                            # Ограничиваем результат 10 строками

# Пояснение:
# Триплет = (субъект, предикат, объект) - базовая единица данных в RDF.
# Этот запрос похож на "SELECT * FROM table LIMIT 10" в SQL, но в SPARQL 
# мы работаем с графом, а не с таблицами.
# Паттерн "?subject ?predicate ?object" находит любые триплеты в графе.
