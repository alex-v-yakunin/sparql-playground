# ADR по источникам данных
#
# Показать, какие ADR из каких именованных графов (источников данных).
# Демонстрирует отслеживание происхождения данных (data provenance).
#
# В SQL: нужна отдельная таблица sources + JOIN
# В SPARQL: Named Graphs - встроенная возможность для провенанса
#
# Ожидаемый результат: 10 строк, показывающих ADR и их источники
# Время: < 1 секунды

PREFIX : <http://example.org/adr#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?source ?adr ?label
WHERE {
    GRAPH ?source {
        ?adr a :ADR ;
             rdfs:label ?label .
    }
}
ORDER BY ?source ?adr

